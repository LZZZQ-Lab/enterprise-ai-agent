# 复制为 secret.yaml 并填入真实值，勿提交密钥到 Git。
#   cp secret.example.yaml secret.yaml
#   kubectl apply -f secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: enterprise-ai-secret
  namespace: enterprise-ai
  labels:
    app.kubernetes.io/name: enterprise-ai-secret
type: Opaque
stringData:
  API_KEY: "replace-me-openai-or-gateway-key"
  VLLM_API_KEY: "EMPTY"
  OPENAI_API_KEY: "replace-me"
